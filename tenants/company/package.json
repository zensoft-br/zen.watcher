{
    "name": "company",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "build": "esbuild src/index.js --bundle --platform=node --outfile=dist/index.js --log-level=debug",
        "deploy": "echo Deploy to AWS Lambda"
    },
    "dependencies": {
        "@zensoftbr/zenerpclient": "^1.0.30"
    }
}
